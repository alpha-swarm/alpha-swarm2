[package]
name = "virt-git"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "In-memory content-addressed git — WASI-portable, no filesystem"

[dependencies]
sha2 = "0.10"
similar = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
