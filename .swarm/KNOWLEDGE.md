# Learned knowledge (SONA)

_Generated snapshot — canonical source is `.swarm/memory/`._

- patterns: 17
- routing shapes: 6
- trajectories: 66

## Distilled patterns (goal→plan that worked)

| reuse | pattern |
|---|---|
| 55× | Add a boolean property accessor to an existing config struct. Create a new impl block with a pub fn method ret |
| 50× | Based on the provided instructions and the tasks outlined, here's how to proceed with each step:  ### Task 1:  |
| 46× | Add documentation to uncovered public fields in a data structure. The plan involved iterating through the fiel |
| 43× | It appears you've provided a set of code snippets in Rust for various modules of an application that seems to  |
| 39× | Create a test module at the end of a source file to validate default configuration values. Added a #[cfg(test) |
| 38× | Add documentation to a specific function in a source code file. The plan involved reviewing related files, the |
| 30× | The task was to add a unit test for the `add` function in the given Rust code. The `add` function takes two in |
| 29× | Add a boolean check method to an existing config struct. Implement the method in a new impl block with a one-l |
| 28× | Add a doc comment to a specific function in a source file. The plan involved directly editing the target file, |
| 27× | Add a boolean status check method to an existing config struct. The plan involved adding a public method with  |
| 26× | Validate default configuration settings. Added a test module with assertions for default values in sequence. U |
| 26× | Add a boolean accessor method to an existing configuration struct. The plan involved adding a new impl block w |
| 25× | Create a modular verification system for default settings. Add a conditional compilation module with test func |
| 25× | Add documentation to an existing function.  The plan involved directly editing the source file, adding a doc c |
| 24× | Refactor code to remove unused mutable references. Removed mut from a closure let-binding, maintaining origina |
| 3× | Add documentation to a Rust struct field.  1. Identify the struct and field requiring documentation. 2. Insert |
| 0× | Implement a new mathematical operation in an existing library module. Added a new function with documentation, |

## Learned routing (UCB1)

| shape | tier | pass | n |
|---|---|---|---|
| complex | agent | 40% | 15 |
| complex | orchestrator | 40% | 10 |
| doc | agent | 81% | 16 |
| doc | orchestrator | 0% | 1 |
| simple | agent | 0% | 1 |
| trivial | orchestrator | 100% | 2 |
