[package]
name = "tool-search"
version = "0.1.0"

[build]
command = "cargo build --target wasm32-wasip2 --release && cp ../../target/wasm32-wasip2/release/tool_search.wasm tool-search.wasm"
artifact = "tool-search.wasm"

[dev]
http_listen_address = "0.0.0.0:8081"
