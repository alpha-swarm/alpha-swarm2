[package]
name = "quality-gate-worker"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
wit-bindgen = "0.41"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
quality-gate-lib = { path = "../../crates/quality-gate", default-features = false }

[lib]
crate-type = ["cdylib"]
